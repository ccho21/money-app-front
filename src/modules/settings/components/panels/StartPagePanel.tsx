'use client';

export default function StartPagePanel() {
  return (
    <div className='p-component bg-surface text-foreground'>
      <p className='text-sm text-muted'>(StartPagePanel) 설정 패널입니다.</p>
    </div>
  );
}
